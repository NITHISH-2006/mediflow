# MediFlow Frontend

A Vite + React + Tailwind hospital management dashboard for the MediFlow backend API.

## Scripts

- npm install
- npm run dev

The app expects the backend API at http://localhost:8080/api.
