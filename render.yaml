services:
  # 1. Backend Web Service (Go + Gin)
  - type: web
    name: mediflow-backend
    runtime: go
    rootDir: backend
    buildCommand: go build -o server main.go
    startCommand: ./server
    envVars:
      - key: GIN_MODE
        value: release
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mediflow-db
          property: connectionString

  # 2. Frontend Static Site (React + Vite)
  - type: web
    name: mediflow-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://mediflow-backend-tmug.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  # 3. PostgreSQL Database
  - name: mediflow-db
    databaseName: mediflow
    user: mediflow_user
    plan: free
